{
  "name": "mobx-psy-packages",
  "version": "1.0.0",
  "private": true,
  "description": "Mobx pseudo syncronous loader",
  "repository": "https://github.com/zerkalica/mobx-psy",
  "author": "Stefan Zerkalica <zerkalica@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "yarn workspaces run clean",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "upgrade": "yarn workspaces run upgrade --all --latest",
    "postinstall": "yarn workspace mobx-psy-config run build"
  },
  "workspaces": [
    "mobx-psy/*",
    "demo/lib/*",
    "demo/*"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.13",
    "@types/styled-system": "^5.1.10",
    "jest": "^26.4.2",
    "mobx-psy-config": "*",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2",
    "yarn": "^1.22.5"
  }
}
