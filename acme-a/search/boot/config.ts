import { acmeSearchBootConfigBrowser } from './configBrowser'

export const acmeSearchBootConfig = {
  browser: acmeSearchBootConfigBrowser,
  port: 9080,
  apiUrl: '/',
  title: 'Search flats INC',
}
