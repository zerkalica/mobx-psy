{
  "name": "@demo/lib-browser",
  "version": "1.0.0",
  "description": "Example browser stuff",
  "main": "-/index.js",
  "types": "-/index.d.ts",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf - *.log",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --passWithNoTests --watch",
    "prepublishOnly": "psy-boot prepublish"
  },
  "dependencies": {
    "@emotion/core": "^10.0.35",
    "@emotion/styled": "^10.0.27",
    "@demo/lib-ui": "^1.0.0",
    "@demo/lib-router": "^1.0.0",
    "mobx": "^5.15.6",
    "@psy/core": "^1.0.0",
    "@psy/mobx-react": "^1.0.0"
  },
  "devDependencies": {
    "@psy/boot": "^1.0.0"
  },
  "peerDependencies": {
    "typescript": "*",
    "jest": "*"
  }
}
