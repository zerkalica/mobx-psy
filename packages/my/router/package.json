{
  "name": "@my/router",
  "version": "1.0.0",
  "description": "Example app stack router",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist *.tsbuildinfo *.log",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "test": "jest"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "mobx-psy": "^1.0.0",
    "mobx": "^5.15.6"
  },
  "peerDependencies": {
    "typescript": "*",
    "jest": "*"
  }
}
