{
  "name": "@psy/mobx-ssr",
  "version": "1.0.0",
  "description": "Mobx psy server side rendering tools",
  "main": "-/index.js",
  "types": "-/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf - *.log",
    "build": "tsc --build tsconfig.build.json",
    "watch": "tsc --build --watch tsconfig.build.json",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --passWithNoTests --watch",
    "prepublishOnly": "psy-boot prepublish"
  },
  "dependencies": {
    "@psy/core": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.10.1",
    "@psy/boot": "^1.0.1"
  },
  "peerDependencies": {
    "react": "*",
    "react-dom": "*",
    "typescript": "*",
    "jest": "*"
  }
}
