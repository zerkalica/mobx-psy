{
  "name": "@psy/ssr",
  "version": "1.0.0",
  "description": "Psy server side rendering tools",
  "main": "-/index.js",
  "types": "-/index.d.ts",
  "exports": {
    "./": "./-/"
  },
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf - *.log",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --passWithNoTests --watch",
    "prepublishOnly": "psy-boot prepublish"
  },
  "dependencies": {
    "@psy/core": "^1.0.0",
    "@psy/context": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.10.1",
    "@psy/boot": "^1.0.1"
  },
  "peerDependencies": {
    "react": "*",
    "react-dom": "*",
    "typescript": "*",
    "jest": "*"
  }
}
